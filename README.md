To use the code, simply do the following in the base terminal containing all the files:

```bash
make
./raytracer1d <input.txt>
```

*Keep in mind this program takes the input file from its home folder and stores the \texttt{.ppm} file in that SAME folder.
